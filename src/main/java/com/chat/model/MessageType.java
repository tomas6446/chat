package com.chat.model;

/**
 * @author Tomas Kozakas
 */
public enum MessageType {
    AUTH_WINDOW,
    LOGIN,
    REGISTER,
    CONNECTED,
    CREATE_ROOM,
    JOIN_ROOM,
    JOINED_ROOM,
    JOIN_PRIVATE,
    SEND,
    RECEIVE,
}
