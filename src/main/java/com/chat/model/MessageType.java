package com.chat.model;

/**
 * @author Tomas Kozakas
 */
public enum MessageType {
    LOGIN,
    REGISTER,
    CONNECTED,
    CREATE_ROOM,
    JOIN_ROOM,
    JOINED_ROOM,
    JOIN_PRIVATE,
    SEND,
    RECEIVE,
}
